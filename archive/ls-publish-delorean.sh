#!/bin/sh
# Praemonitus praemunitus: This script is a hack that works for me and is part
# of a whole set of scripts. I'm only putting it here to remind myself to fix
# this if need be and upload
# The following commands need to run as root since that is what vagrant runs as root
sudo cp ~/src/lightspeed/lightspeed/build/artifacts/DeloreanServer-3.8-SNAPSHOT.zip ~/src/firewalker/cloud-ws-ravi/

